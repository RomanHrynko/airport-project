const initialState = {
  flightsList: [],
};

const flightsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default flightsReducer;
